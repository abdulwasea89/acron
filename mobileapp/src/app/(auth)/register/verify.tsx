import { View, Text } from "@/tw";

export default function Screen_verify() {
  return (
    <View className="flex-1 items-center justify-center bg-white dark:bg-bg-dark">
      <Text className="text-lg text-gray-900 dark:text-white">./(auth)/register/verify</Text>
    </View>
  );
}
