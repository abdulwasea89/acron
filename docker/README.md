# Docker

Container definitions for the Gym Platform.

| File | Purpose |
|------|---------|
| `Dockerfile` | Multi-stage production build for the FastAPI backend |
| `docker-compose.prod.yml` | Production Compose file (backend + workers + nginx + postgres + redis) |
