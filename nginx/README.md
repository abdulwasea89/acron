# Nginx

Reverse proxy configuration for the Gym Platform.

| File | Purpose |
|------|---------|
| `nginx.conf` | Main config — routes `/api` to backend, `/` to frontend, handles SSL termination, rate limiting, static file serving |
